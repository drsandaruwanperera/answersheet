import {db} from '../firebase.js';import{collection,addDoc,deleteDoc,doc,onSnapshot,query,orderBy,updateDoc,serverTimestamp}from'https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js';const s=document.currentScript,cfg=JSON.parse(s.dataset.config),fields=cfg.fields,form=document.getElementById('form'),list=document.getElementById('list'),msg=document.getElementById('msg'),reset=document.getElementById('resetBtn'),title=document.getElementById('formTitle');let editId=null;const esc=x=>String(x??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));function message(t,e=false){msg.textContent=t;msg.style.color=e?'#c73555':'#4037c9'}function clear(){editId=null;form.reset();title.textContent='Add '+cfg.title;message('')}function read(){const o={};for(const f of fields){const el=document.getElementById('f_'+f.key);o[f.key]=f.type==='checkbox'?el.checked:el.value.trim();if(f.type==='number'&&o[f.key]!=='')o[f.key]=Number(o[f.key])}return o}function fill(id,d){editId=id;for(const f of fields){const el=document.getElementById('f_'+f.key);if(f.type==='checkbox')el.checked=d[f.key]===true;else el.value=d[f.key]??''}title.textContent='Edit '+cfg.title;scrollTo({top:0,behavior:'smooth'})}form.addEventListener('submit',async e=>{e.preventDefault();try{const data=read();data.updatedAt=serverTimestamp();if(editId)await updateDoc(doc(db,cfg.collection,editId),data);else{data.createdAt=serverTimestamp();await addDoc(collection(db,cfg.collection),data)}message('Saved successfully.');clear()}catch(err){console.error(err);message('Could not save: '+err.message,true)}});reset?.addEventListener('click',clear);function render(snap){list.innerHTML='';if(snap.empty){list.innerHTML='<div class="empty">No items yet.</div>';return}snap.docs.forEach(d=>{const x=d.data(),div=document.createElement('div');div.className='item';const first=fields.find(f=>f.type!=='checkbox');div.innerHTML='<h3>'+esc(x[first?.key]||cfg.title)+'</h3>'+fields.filter(f=>f.key!==first?.key&&f.key!=='enabled').map(f=>'<p><strong>'+esc(f.key)+':</strong> '+esc(x[f.key])+'</p>').join('')+'<p><strong>Visible:</strong> '+(x.enabled===false?'No':'Yes')+'</p><div class="item-actions"><button class="btn secondary edit">Edit</button><button class="btn danger delete">Delete</button></div>';div.querySelector('.edit').onclick=()=>fill(d.id,x);div.querySelector('.delete').onclick=async()=>{if(confirm('Delete this item?'))await deleteDoc(doc(db,cfg.collection,d.id))};list.appendChild(div)})}onSnapshot(query(collection(db,cfg.collection),orderBy('order','asc')),render,e=>{console.error(e);list.innerHTML='<div class="empty">Unable to load items.</div>'});